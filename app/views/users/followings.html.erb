<h1><%= t('users.followings.following') %></h1>
<% if @users.present? %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to "#{user.name}", user_path("#{user.id}") %></li>
    <% end %>
  </ul>
<% else %>
  <p><%= t('users.followers.notice') %></p>
<% end %>

<%= paginate @users %>
