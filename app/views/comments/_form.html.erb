<h3><%= Comment.human_attribute_name(:addition) %></h3>
<%= form_with(model: [commentable, Comment.new]) do |form| %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :body, required: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
